import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export default function modelProviderStatus(_pi: ExtensionAPI): void {
	// Superseded by extensions/sub-usage-lite.ts so the provider icon appears on
	// the already-visible usage status line instead of as a separate footer entry.
}
